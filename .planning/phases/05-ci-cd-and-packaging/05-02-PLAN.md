---
phase: 05-ci-cd-and-packaging
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - .github/workflows/release.yml
autonomous: true
requirements:
  - PKG-02

must_haves:
  truths:
    - "release.yml triggers only on v* tag pushes, not on regular pushes or PRs"
    - "release.yml builds a statically-linked binary using x86_64-unknown-linux-musl target"
    - "release binary is named caldawarrior-v{version}-x86_64-linux"
    - "SHA256 checksum file is generated alongside the binary"
    - "GitHub Release is created with the binary and checksum as assets"
  artifacts:
    - path: ".github/workflows/release.yml"
      provides: "Tag-triggered release workflow for binary publishing"
      contains: "x86_64-unknown-linux-musl"
  key_links:
    - from: ".github/workflows/release.yml"
      to: "Cargo.toml"
      via: "cargo build --release --target x86_64-unknown-linux-musl reads Cargo.toml"
      pattern: "cargo build --release"
    - from: ".github/workflows/release.yml"
      to: "GitHub Releases"
      via: "softprops/action-gh-release uploads binary assets"
      pattern: "action-gh-release"
---

<objective>
Create the release workflow that builds and publishes a pre-built x86_64-linux static binary to GitHub Releases when a version tag is pushed.

Purpose: PKG-02 requires that tagging a release version triggers automatic build and publication of a pre-built x86_64-linux binary to GitHub Releases. This is the final piece of the CI/CD pipeline.

Output: .github/workflows/release.yml
</objective>

<execution_context>
@/home/alexandre/.claude/get-shit-done/workflows/execute-plan.md
@/home/alexandre/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ci-cd-and-packaging/05-CONTEXT.md
@.planning/phases/05-ci-cd-and-packaging/05-RESEARCH.md
@.planning/phases/05-ci-cd-and-packaging/05-01-SUMMARY.md
</context>

<interfaces>
<!-- From Plan 01: Cargo.toml must already have default-features = false on reqwest -->
<!-- This is required for the MUSL static build to succeed (no OpenSSL dependency) -->

From Cargo.toml (after Plan 01 fix):
```toml
reqwest = { version = "0.12", default-features = false, features = ["blocking", "rustls-tls"] }
```

From .github/workflows/ci.yml (created in Plan 01):
```yaml
# ci.yml uses dtolnay/rust-toolchain@stable and Swatinem/rust-cache@v2
# release.yml follows the same action conventions
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create release workflow for tag-triggered binary publishing</name>
  <files>.github/workflows/release.yml</files>
  <read_first>
    Cargo.toml, .github/workflows/ci.yml
  </read_first>
  <action>
    Create `.github/workflows/release.yml` with the following exact content:

    ```yaml
    name: Release

    on:
      push:
        tags:
          - "v*"

    permissions:
      contents: write

    jobs:
      release:
        name: Build and Publish
        runs-on: ubuntu-latest
        steps:
          - uses: actions/checkout@v4
          - uses: dtolnay/rust-toolchain@stable
            with:
              targets: x86_64-unknown-linux-musl
          - name: Install musl-tools
            run: sudo apt-get update && sudo apt-get install -y musl-tools
          - uses: Swatinem/rust-cache@v2
            with:
              key: release-musl
          - name: Build static binary
            run: cargo build --release --target x86_64-unknown-linux-musl
          - name: Prepare release assets
            run: |
              VERSION="${GITHUB_REF_NAME}"
              BINARY_NAME="caldawarrior-${VERSION}-x86_64-linux"
              cp target/x86_64-unknown-linux-musl/release/caldawarrior "${BINARY_NAME}"
              sha256sum "${BINARY_NAME}" > "${BINARY_NAME}.sha256"
          - name: Publish to GitHub Releases
            uses: softprops/action-gh-release@v2
            with:
              generate_release_notes: true
              files: |
                caldawarrior-v*-x86_64-linux
                caldawarrior-v*-x86_64-linux.sha256
    ```

    Key design decisions (all from CONTEXT.md locked decisions):
    - **Tag pattern:** `v*` semver (e.g., v1.0.0, v1.2.3-rc1) -- per user decision
    - **Binary naming:** `caldawarrior-v{version}-x86_64-linux` (version baked into filename) -- per user decision
    - **Standalone binary:** No tar.gz archive, single statically-linked executable -- per user decision
    - **Checksum:** SHA256 file alongside binary (`caldawarrior-v{version}-x86_64-linux.sha256`) -- per user decision
    - **Separate workflow file:** `release.yml` triggered only on `v*` tags -- per user decision
    - **MUSL target:** `x86_64-unknown-linux-musl` for true static linking (no glibc dependency) -- per research recommendation
    - **musl-tools:** Required for the MUSL linker on Ubuntu runner
    - **Cache key:** `release-musl` to separate release cache from CI cache (different target)
    - **permissions: contents: write:** Required for softprops/action-gh-release to create releases and upload assets
    - **generate_release_notes: true:** Auto-generates release notes from commits since last tag

    After creating, verify the MUSL build works locally:
    ```bash
    # If musl target is installed locally:
    rustup target add x86_64-unknown-linux-musl
    cargo build --release --target x86_64-unknown-linux-musl
    file target/x86_64-unknown-linux-musl/release/caldawarrior
    # Should show "statically linked" in the output
    ```

    If musl-tools is not installed locally, the workflow file structure is verified by reading ci.yml for consistent patterns (same checkout, toolchain, cache actions).
  </action>
  <verify>
    <automated>test -f .github/workflows/release.yml && grep -q 'x86_64-unknown-linux-musl' .github/workflows/release.yml && grep -q 'action-gh-release' .github/workflows/release.yml && grep -q 'sha256sum' .github/workflows/release.yml && grep -q 'tags:' .github/workflows/release.yml</automated>
  </verify>
  <acceptance_criteria>
    - .github/workflows/release.yml exists
    - release.yml `on.push.tags` contains `"v*"` pattern
    - release.yml does NOT trigger on regular push or pull_request events
    - release.yml has `permissions: contents: write`
    - release.yml uses `dtolnay/rust-toolchain@stable` with `targets: x86_64-unknown-linux-musl`
    - release.yml contains `sudo apt-get install -y musl-tools`
    - release.yml contains `cargo build --release --target x86_64-unknown-linux-musl`
    - release.yml contains `BINARY_NAME="caldawarrior-${VERSION}-x86_64-linux"`
    - release.yml contains `sha256sum "${BINARY_NAME}" > "${BINARY_NAME}.sha256"`
    - release.yml uses `softprops/action-gh-release@v2`
    - release.yml contains `generate_release_notes: true`
    - release.yml uploads both `caldawarrior-v*-x86_64-linux` and `caldawarrior-v*-x86_64-linux.sha256`
    - release.yml uses `Swatinem/rust-cache@v2` with `key: release-musl`
  </acceptance_criteria>
  <done>release.yml created with tag-triggered MUSL static build, binary+checksum asset upload to GitHub Releases via softprops/action-gh-release.</done>
</task>

</tasks>

<verification>
- `.github/workflows/release.yml` exists and is valid YAML
- release.yml triggers only on `v*` tags (not on push/PR)
- release.yml builds with MUSL target for static binary
- release.yml names binary as `caldawarrior-v{version}-x86_64-linux`
- release.yml generates SHA256 checksum file
- release.yml publishes to GitHub Releases with both files
</verification>

<success_criteria>
PKG-02 is satisfied: release.yml is configured to build a statically-linked x86_64-linux binary and publish it with a SHA256 checksum to GitHub Releases when a v* tag is pushed. The actual release workflow will be tested by pushing a tag after merging to the remote.
</success_criteria>

<output>
After completion, create `.planning/phases/05-ci-cd-and-packaging/05-02-SUMMARY.md`
</output>
