---
phase: 07-readme-accuracy-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - DOC-01
must_haves:
  truths:
    - "README Limitation 12 describes annotation-to-DESCRIPTION sync as implemented (first annotation only)"
    - "README Field Mapping table includes an annotations-to-DESCRIPTION row"
    - "README v2 Roadmap does NOT list Annotation/DESCRIPTION sync as a planned feature"
    - "README Compatibility section links to docs/compatibility/tasks-org.md"
  artifacts:
    - path: "README.md"
      provides: "Accurate documentation reflecting all implemented features"
      contains: "annotations.*DESCRIPTION"
  key_links:
    - from: "README.md Compatibility section"
      to: "docs/compatibility/tasks-org.md"
      via: "relative Markdown link"
      pattern: "tasks-org\\.md"
---

<objective>
Fix four documentation inaccuracies in README.md identified by the v1.0 milestone audit (MISS-01, FLOW-01). After Phase 3 implemented annotation-to-DESCRIPTION bidirectional sync, the README (written in Phase 6) still claims this feature is absent. This plan makes four surgical edits to correct Limitation 12, add a Field Mapping row, remove a stale v2 Roadmap entry, and add a missing compatibility doc link.

Purpose: Ensure README accurately reflects all implemented features so new users are not misled about capabilities.
Output: Corrected README.md with all four edits applied.
</objective>

<execution_context>
@/home/alexandre/.claude/get-shit-done/workflows/execute-plan.md
@/home/alexandre/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-readme-accuracy-fixes/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct Limitation 12 and add Field Mapping row</name>
  <files>README.md</files>
  <read_first>
    - README.md (current state of all edit targets)
    - src/mapper/fields.rs (lines 60-80, confirms first-annotation-only behavior at line 72)
  </read_first>
  <action>
Make two edits to README.md:

**Edit 1 — Rewrite Limitation 12 (currently lines 390-396):**

Replace the entire Limitation 12 block:

```
### 12. No description or annotation sync

TaskWarrior annotations are not mapped to any CalDAV field. The CalDAV `DESCRIPTION` property
is not imported into TaskWarrior. Only the `SUMMARY` field (mapped to `description`) is synced.

**Workaround:** Keep extended notes in one system only. Neither side will overwrite or delete
the other's extended notes; they simply remain invisible across the sync boundary.
```

With this corrected text:

```
### 12. Only first annotation synced to DESCRIPTION

The first TaskWarrior annotation is mapped bidirectionally to the CalDAV `DESCRIPTION` property.
Only the first annotation is synced; additional annotations are ignored. If a task has multiple
annotations, only the first one is visible on the CalDAV side.

**Workaround:** Keep essential notes in the first annotation. Use subsequent annotations for
local-only context that does not need to cross the sync boundary.
```

Do NOT change the limitation number (keep it as 12). Do NOT renumber any other limitations.

**Edit 2 — Add annotations row to Field Mapping table (currently line 220):**

Insert a new row in the Field Mapping table immediately after the `description | SUMMARY` row (line 212) and before the `due | DUE` row (line 213). The new row is:

```
| `annotations[0]`  | `DESCRIPTION`                  | First annotation only        |
```

The resulting table should have the rows in this order:
1. `description` -> `SUMMARY`
2. `annotations[0]` -> `DESCRIPTION` (NEW)
3. `due` -> `DUE`
4. (rest unchanged)
  </action>
  <verify>
    <automated>grep -n 'Only first annotation synced' README.md && grep -n 'annotations\[0\].*DESCRIPTION' README.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - README.md line matching `### 12. Only first annotation synced to DESCRIPTION` exists
    - README.md contains `The first TaskWarrior annotation is mapped bidirectionally to the CalDAV`
    - README.md contains `| \x60annotations[0]\x60  | \x60DESCRIPTION\x60` in the Field Mapping table
    - README.md does NOT contain `### 12. No description or annotation sync`
    - README.md does NOT contain `TaskWarrior annotations are not mapped to any CalDAV field`
    - Limitation numbering is still 1-15 with no gaps or renumbering
  </acceptance_criteria>
  <done>Limitation 12 accurately describes first-annotation-only bidirectional sync. Field Mapping table includes annotations[0] -> DESCRIPTION row.</done>
</task>

<task type="auto">
  <name>Task 2: Remove stale v2 Roadmap entry and add tasks-org.md link</name>
  <files>README.md</files>
  <read_first>
    - README.md (current state after Task 1 edits)
    - docs/compatibility/tasks-org.md (verify link target exists)
  </read_first>
  <action>
Make two edits to README.md:

**Edit 3 — Remove v2 Roadmap "Annotation / DESCRIPTION sync" row (currently line 488):**

Delete the entire row from the v2 Roadmap table:

```
| **Annotation / DESCRIPTION sync** | Map TaskWarrior annotations to CalDAV `DESCRIPTION` and vice versa |
```

The remaining 6 rows in the v2 Roadmap table stay unchanged:
1. Sync token (RFC 6578)
2. Keyring integration
3. DIGEST auth
4. Multi-server support
5. CalDAV CANCEL recovery
6. Field-level conflict merging

**Edit 4 — Add tasks-org.md link in Compatibility section:**

In the Compatibility section, at the end of the DEPENDS-ON note paragraph (the paragraph starting with `*\*DEPENDS-ON note:*`), add a new sentence before the paragraph ends. The paragraph currently ends with:

```
See [Known Limitation #15](#15-depends-on-relations-invisible-to-caldav-clients).
```

After that sentence, on a new line, add:

```
For detailed tasks.org and DAVx5 compatibility analysis, see [tasks.org / DAVx5 Compatibility](docs/compatibility/tasks-org.md).
```

So the full end of the DEPENDS-ON note paragraph becomes:

```
See [Known Limitation #15](#15-depends-on-relations-invisible-to-caldav-clients).
For detailed tasks.org and DAVx5 compatibility analysis, see [tasks.org / DAVx5 Compatibility](docs/compatibility/tasks-org.md).
```
  </action>
  <verify>
    <automated>grep -c 'Annotation.*DESCRIPTION sync' README.md | grep -q '^0$' && grep -n 'tasks-org.md' README.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Annotation / DESCRIPTION sync' README.md` returns 0 (row removed)
    - README.md v2 Roadmap table has exactly 6 rows (not 7)
    - README.md contains `[tasks.org / DAVx5 Compatibility](docs/compatibility/tasks-org.md)`
    - The tasks-org.md link appears in the Compatibility section, not in some other section
    - The 6 remaining v2 Roadmap entries are: Sync token, Keyring integration, DIGEST auth, Multi-server support, CalDAV CANCEL recovery, Field-level conflict merging
  </acceptance_criteria>
  <done>v2 Roadmap no longer lists annotation sync as planned. Compatibility section links to tasks-org.md for detailed analysis.</done>
</task>

</tasks>

<verification>
All four success criteria from the ROADMAP phase definition:

1. `grep 'Only first annotation synced' README.md` -- Limitation 12 corrected
2. `grep 'annotations\[0\].*DESCRIPTION' README.md` -- Field Mapping row present
3. `grep -c 'Annotation / DESCRIPTION sync' README.md` returns 0 -- v2 entry removed
4. `grep 'tasks-org.md' README.md` -- Compatibility link present
</verification>

<success_criteria>
- All four grep verification commands pass
- README.md has no references to annotation sync being absent or planned
- README.md accurately describes annotation[0] <-> DESCRIPTION as implemented with first-annotation-only limitation
- Limitation numbering 1-15 remains intact
- docs/compatibility/tasks-org.md link is reachable (relative path from repo root)
</success_criteria>

<output>
After completion, create `.planning/phases/07-readme-accuracy-fixes/07-01-SUMMARY.md`
</output>
