---
phase: 06-documentation-and-release
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - Cargo.toml
autonomous: true
requirements:
  - DOC-01
  - DOC-02
  - DOC-04

must_haves:
  truths:
    - "README has Installation section with pre-built binary download as first option, cargo install second, build-from-source third"
    - "README has Config Reference section listing all 6 config options with type, default, required, and description"
    - "README has Scheduling section with cron (with flock) and systemd timer/service examples"
    - "README has Compatibility section with three-tier server and client matrix"
    - "README has Known Limitation #15 about DEPENDS-ON client invisibility"
    - "README CLI Reference includes --fail-fast flag"
    - "Cargo.toml version is 1.0.0"
    - "cargo build --release succeeds after version bump"
  artifacts:
    - path: "README.md"
      provides: "Complete user documentation"
      contains: "## Installation"
    - path: "README.md"
      provides: "Config reference"
      contains: "## Config Reference"
    - path: "README.md"
      provides: "Scheduling guide"
      contains: "## Scheduling"
    - path: "README.md"
      provides: "Compatibility matrix"
      contains: "## Compatibility"
    - path: "README.md"
      provides: "Limitation 15"
      contains: "### 15."
    - path: "Cargo.toml"
      provides: "Release version"
      contains: 'version = "1.0.0"'
  key_links:
    - from: "README.md Installation section"
      to: ".github/workflows/release.yml"
      via: "Binary naming convention"
      pattern: "caldawarrior-v.*-x86_64-linux"
    - from: "README.md Config Reference"
      to: "src/config.rs"
      via: "Struct fields match documented options"
      pattern: "server_url.*username.*password.*completed_cutoff_days.*allow_insecure_tls.*caldav_timeout_seconds"
---

<objective>
Update README.md with complete user documentation (Installation, Config Reference, Scheduling, Compatibility matrix, limitation #15, updated CLI Reference) and bump Cargo.toml version from 0.1.0 to 1.0.0 for release readiness.

Purpose: A new user must be able to install, configure, and use caldawarrior from the README alone, with known limitations and server/client compatibility clearly documented.
Output: Updated README.md with all required sections, Cargo.toml at version 1.0.0, verified build.
</objective>

<execution_context>
@/home/alexandre/.claude/get-shit-done/workflows/execute-plan.md
@/home/alexandre/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-documentation-and-release/06-CONTEXT.md
@.planning/phases/06-documentation-and-release/06-RESEARCH.md

<interfaces>
<!-- Source of truth for Config Reference (DOC-02) -->

From src/config.rs:
```rust
pub struct CalendarEntry {
    pub project: String,
    pub url: String,
}

pub struct Config {
    pub server_url: String,
    pub username: String,
    pub password: String,
    #[serde(default = "default_completed_cutoff_days")]
    pub completed_cutoff_days: u32,        // default: 90
    #[serde(default)]
    pub allow_insecure_tls: bool,          // default: false
    #[serde(default = "default_caldav_timeout_seconds")]
    pub caldav_timeout_seconds: u64,       // default: 30
    #[serde(default, rename = "calendar")]
    pub calendars: Vec<CalendarEntry>,
}
```

Config path resolution order:
1. `--config` CLI flag
2. `CALDAWARRIOR_CONFIG` environment variable
3. `~/.config/caldawarrior/config.toml`

Env vars: CALDAWARRIOR_PASSWORD (overrides password field), CALDAWARRIOR_CONFIG (alternative config path)

<!-- Binary naming convention from release workflow -->

From .github/workflows/release.yml:
```yaml
VERSION="${GITHUB_REF_NAME}"    # e.g., "v1.0.0"
BINARY_NAME="caldawarrior-${VERSION}-x86_64-linux"
```

Download URL pattern: `https://github.com/alexandrebarsacq/caldawarrior/releases/latest/download/caldawarrior-v{VERSION}-x86_64-linux`

<!-- CLI flags from src/main.rs -->
sync subcommand flags: --dry-run, --fail-fast
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update README.md with Installation, Config Reference, Scheduling, Compatibility, and limitation #15</name>
  <files>README.md</files>
  <read_first>
    - README.md (current content, 361 lines -- update in place, do not rewrite from scratch)
    - src/config.rs (lines 1-35 for struct fields and defaults -- source of truth for Config Reference)
    - .github/workflows/release.yml (lines 28-31 for binary naming convention)
    - src/main.rs (lines 25-35 for CLI flags including --fail-fast)
    - docs/compatibility/tasks-org.md (DEPENDS-ON visibility matrix for compatibility section)
    - .planning/phases/06-documentation-and-release/06-RESEARCH.md (section ordering, code examples, compatibility evidence)
  </read_first>
  <action>
Update README.md in-place with the following changes. Preserve all existing content that is not being replaced. The target section ordering is:

1. Title + description (KEEP existing)
2. Features (KEEP existing)
3. **Installation** (NEW section, replaces "Step 1 -- Install" content from Quick Start)
4. Quick Start (RESTRUCTURE: remove Step 1 install content, renumber remaining steps 1-4)
5. **Config Reference** (NEW section -- DOC-02)
6. CLI Reference (KEEP existing, but ADD --fail-fast flag)
7. **Scheduling** (NEW section)
8. Field Mapping + Status Mapping (KEEP existing)
9. **Compatibility** (NEW section -- DOC-04)
10. v1 Known Limitations (KEEP existing 1-14, ADD #15)
11. Testing (KEEP existing)
12. v2 Roadmap (KEEP existing)
13. License (KEEP existing)

**Installation section** (NEW -- insert before Quick Start):

```markdown
## Installation

### Pre-built Binary (Recommended)

Download the latest release for x86_64 Linux:

\`\`\`bash
# Download binary and checksum
curl -LO https://github.com/alexandrebarsacq/caldawarrior/releases/latest/download/caldawarrior-v1.0.0-x86_64-linux
curl -LO https://github.com/alexandrebarsacq/caldawarrior/releases/latest/download/caldawarrior-v1.0.0-x86_64-linux.sha256

# Verify checksum
sha256sum -c caldawarrior-v1.0.0-x86_64-linux.sha256

# Install
chmod +x caldawarrior-v1.0.0-x86_64-linux
sudo mv caldawarrior-v1.0.0-x86_64-linux /usr/local/bin/caldawarrior
\`\`\`

Check the [Releases page](https://github.com/alexandrebarsacq/caldawarrior/releases) for the latest version.

### cargo install

\`\`\`bash
cargo install --git https://github.com/alexandrebarsacq/caldawarrior.git
\`\`\`

### Build from Source

\`\`\`bash
git clone https://github.com/alexandrebarsacq/caldawarrior.git
cd caldawarrior
cargo build --release
# binary is at target/release/caldawarrior
\`\`\`
```

**Quick Start restructure:** Remove the current "Step 1 -- Install" block (lines 47-60 of current README). Renumber remaining steps:
- Step 1 -- Configure (was Step 2)
- Step 2 -- Register the TaskWarrior UDA (was Step 3)
- Step 3 -- Preview with dry-run (was Step 4)
- Step 4 -- First live sync (was Step 5)

Also update the clone URL in the build-from-source section from `https://github.com/example/caldawarrior` to `https://github.com/alexandrebarsacq/caldawarrior`.

**Config Reference section** (NEW -- insert after Quick Start, before CLI Reference):

```markdown
## Config Reference

### Options

| Option | Type | Default | Required | Description |
|--------|------|---------|----------|-------------|
| `server_url` | string | -- | Yes | Base URL of the CalDAV server |
| `username` | string | -- | Yes | CalDAV username for authentication |
| `password` | string | -- | Yes | CalDAV password (see Environment Variables below) |
| `completed_cutoff_days` | integer | `90` | No | Number of days of completed/deleted task history to include in sync |
| `allow_insecure_tls` | boolean | `false` | No | Skip TLS certificate verification (for self-signed certificates) |
| `caldav_timeout_seconds` | integer | `30` | No | HTTP request timeout in seconds for CalDAV operations |

### Calendar Entries

Each `[[calendar]]` section maps a TaskWarrior project to a CalDAV collection:

| Option | Type | Required | Description |
|--------|------|----------|-------------|
| `project` | string | Yes | TaskWarrior project name |
| `url` | string | Yes | Full URL of the CalDAV calendar collection |

### Environment Variables

| Variable | Description |
|----------|-------------|
| `CALDAWARRIOR_PASSWORD` | Overrides the `password` field in config.toml |
| `CALDAWARRIOR_CONFIG` | Path to config file (overrides default `~/.config/caldawarrior/config.toml`) |

### Config Path Resolution

1. `--config` CLI flag (highest priority)
2. `CALDAWARRIOR_CONFIG` environment variable
3. `~/.config/caldawarrior/config.toml` (default)
```

**CLI Reference update:** Replace the existing CLI Reference block with:

```markdown
## CLI Reference

\`\`\`
caldawarrior [--config PATH] <SUBCOMMAND>

Options:
  --config PATH   Path to config file (overrides CALDAWARRIOR_CONFIG env var and default)

Subcommands:
  sync            Sync TaskWarrior tasks with CalDAV
    --dry-run     Preview changes without writing anything
    --fail-fast   Stop on first sync error instead of continuing
\`\`\`
```

**Scheduling section** (NEW -- insert after CLI Reference, before Field Mapping):

```markdown
## Scheduling

### Cron

\`\`\`bash
# Sync every 15 minutes with flock to prevent overlapping runs
*/15 * * * * /usr/bin/flock -n /tmp/caldawarrior.lock /usr/local/bin/caldawarrior sync >> /var/log/caldawarrior.log 2>&1
\`\`\`

### Systemd Timer

Create two files:

**`~/.config/systemd/user/caldawarrior.service`**
\`\`\`ini
[Unit]
Description=Sync TaskWarrior with CalDAV

[Service]
Type=oneshot
ExecStart=/usr/local/bin/caldawarrior sync
\`\`\`

**`~/.config/systemd/user/caldawarrior.timer`**
\`\`\`ini
[Unit]
Description=Run caldawarrior sync periodically

[Timer]
OnBootSec=1min
OnUnitActiveSec=15min
Persistent=true

[Install]
WantedBy=timers.target
\`\`\`

Enable and start:
\`\`\`bash
systemctl --user enable --now caldawarrior.timer
\`\`\`
```

**Compatibility section** (NEW -- insert after Status Mapping, before Known Limitations):

```markdown
## Compatibility

### Servers

| Server | Tier | Notes |
|--------|------|-------|
| Radicale | Tested | Full E2E test suite runs against Radicale. All features verified. |
| Nextcloud | Expected | XML parser handles Nextcloud namespaces. Weak ETag normalization implemented. Not E2E tested. |
| Baikal | Expected | Standard CalDAV compliance. Not E2E tested. |

### Clients

| Client | Tier | Notes |
|--------|------|-------|
| TaskWarrior 3.x | Tested | Primary sync target. Full bidirectional sync verified. |
| tasks.org + DAVx5 | Tested* | Basic VTODO sync works. See DEPENDS-ON note below. |
| Thunderbird | Expected | CalDAV VTODO support available. Not tested with caldawarrior. |

**Tiers:** *Tested* = verified with E2E test suite. *Expected* = should work based on standards compliance, not E2E tested. *Unknown* = not evaluated.

*\*DEPENDS-ON note:* caldawarrior syncs task dependencies using `RELATED-TO;RELTYPE=DEPENDS-ON` ([RFC 9253](https://datatracker.ietf.org/doc/html/rfc9253)). This property is preserved through sync by Radicale and DAVx5, but no tested CalDAV client currently renders DEPENDS-ON relationships in its UI. Dependencies work fully between TaskWarrior instances syncing through a CalDAV server. See [Known Limitation #15](#15-depends-on-relations-invisible-to-caldav-clients).
```

**Known Limitation #15** (NEW -- append after limitation #14, before Testing section):

```markdown
### 15. DEPENDS-ON relations invisible to CalDAV clients

Task dependencies synced via `RELATED-TO;RELTYPE=DEPENDS-ON` (RFC 9253) are preserved on the
CalDAV server but not displayed by any tested client (tasks.org, Thunderbird). Dependencies
work correctly between TaskWarrior instances syncing through the same CalDAV server.

**Workaround:** Use TaskWarrior directly to view and manage task dependencies. CalDAV clients
will show tasks individually without dependency relationships.

---
```
  </action>
  <verify>
    <automated>grep -c '## Installation' README.md && grep -c '## Config Reference' README.md && grep -c '## Scheduling' README.md && grep -c '## Compatibility' README.md && grep -c '### 15\.' README.md && grep -c 'fail-fast' README.md && grep -c 'caldawarrior-v1.0.0-x86_64-linux' README.md && grep -c 'CALDAWARRIOR_PASSWORD' README.md && grep -c 'completed_cutoff_days' README.md</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains `## Installation` section with three subsections: `### Pre-built Binary`, `### cargo install`, `### Build from Source`
    - README.md contains `## Config Reference` with `### Options` table listing exactly 6 options (server_url, username, password, completed_cutoff_days, allow_insecure_tls, caldav_timeout_seconds)
    - README.md contains `### Calendar Entries` subsection with project and url fields
    - README.md contains `### Environment Variables` with CALDAWARRIOR_PASSWORD and CALDAWARRIOR_CONFIG
    - README.md contains `### Config Path Resolution` with 3 numbered steps
    - README.md contains `## Scheduling` with `### Cron` (including flock) and `### Systemd Timer` subsections
    - README.md contains `## Compatibility` with `### Servers` table (Radicale Tested, Nextcloud Expected, Baikal Expected) and `### Clients` table (TaskWarrior 3.x Tested, tasks.org + DAVx5 Tested*, Thunderbird Expected)
    - README.md contains `### 15. DEPENDS-ON relations invisible to CalDAV clients` with workaround
    - README.md CLI Reference contains `--fail-fast` line with description `Stop on first sync error instead of continuing`
    - README.md Quick Start begins with Step 1 -- Configure (not Install)
    - README.md Installation section contains URL `https://github.com/alexandrebarsacq/caldawarrior` (not example/caldawarrior)
    - README.md does NOT contain `https://github.com/example/caldawarrior`
    - All 14 original known limitations are preserved (grep for `### 1.` through `### 14.`)
  </acceptance_criteria>
  <done>README.md contains Installation (3 methods, binary first), Config Reference (6 options + calendar entries + env vars + path resolution), CLI Reference with --fail-fast, Scheduling (cron + systemd), Compatibility (3-tier matrix with DEPENDS-ON footnote), and Known Limitation #15. Quick Start renumbered. All existing content preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Bump Cargo.toml version to 1.0.0 and verify build</name>
  <files>Cargo.toml</files>
  <read_first>
    - Cargo.toml (current content, verify version is 0.1.0 on line 3)
  </read_first>
  <action>
Edit Cargo.toml line 3: change `version = "0.1.0"` to `version = "1.0.0"`.

No other changes to Cargo.toml.

After editing, run `cargo build --release` to verify the version bump does not break the build. The release workflow cache key (release-musl) does not include the version, so no caching issues expected.

Do NOT create a git tag. The user will tag manually when ready to release.
  </action>
  <verify>
    <automated>grep -q 'version = "1.0.0"' Cargo.toml && cargo build --release 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - Cargo.toml contains exactly `version = "1.0.0"` (not `0.1.0`)
    - `cargo build --release` exits with code 0
    - No git tag created (verify with `git tag -l 'v1*'` returning empty)
  </acceptance_criteria>
  <done>Cargo.toml version is 1.0.0. Release build succeeds. No tag created.</done>
</task>

</tasks>

<verification>
1. `grep '## Installation' README.md` -- section exists
2. `grep '## Config Reference' README.md` -- section exists
3. `grep '## Scheduling' README.md` -- section exists
4. `grep '## Compatibility' README.md` -- section exists
5. `grep '### 15\.' README.md` -- limitation 15 exists
6. `grep 'fail-fast' README.md` -- CLI flag documented
7. `grep 'version = "1.0.0"' Cargo.toml` -- version bumped
8. `cargo build --release` -- build succeeds
9. `grep -c 'CALDAWARRIOR_PASSWORD' README.md` -- env var documented (should be >= 2: config reference + existing mentions)
10. `grep 'alexandrebarsacq/caldawarrior' README.md | head -3` -- correct repo URL used
</verification>

<success_criteria>
- README.md has all 4 new sections (Installation, Config Reference, Scheduling, Compatibility)
- Config Reference documents exactly the 6 fields from src/config.rs with correct types and defaults
- Compatibility matrix has Radicale (Tested), Nextcloud (Expected), Baikal (Expected) servers and TW 3.x (Tested), tasks.org+DAVx5 (Tested*), Thunderbird (Expected) clients
- Known Limitation #15 added about DEPENDS-ON invisibility
- CLI Reference includes --fail-fast
- Quick Start renumbered (Steps 1-4, starting with Configure)
- Cargo.toml version is 1.0.0
- cargo build --release succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/06-documentation-and-release/06-01-SUMMARY.md`
</output>
