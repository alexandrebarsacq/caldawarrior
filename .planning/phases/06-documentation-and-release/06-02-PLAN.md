---
phase: 06-documentation-and-release
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - CHANGELOG.md
autonomous: true
requirements:
  - DOC-03

must_haves:
  truths:
    - "CHANGELOG.md exists in repository root"
    - "CHANGELOG follows Keep a Changelog 1.1.0 format with header, preamble, and version section"
    - "Version header is ## [1.0.0] - 2026-03-XX (date placeholder)"
    - "Entries are grouped under Added, Changed, and Fixed subsections"
    - "Entries describe user-facing changes, not internal commits"
    - "No internal planning, docs, or test-infrastructure commits appear as entries"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "Release changelog"
      contains: "## [1.0.0]"
      min_lines: 30
  key_links:
    - from: "CHANGELOG.md"
      to: "git log"
      via: "Hand-curated commit-to-entry mapping"
      pattern: "Added|Changed|Fixed"
---

<objective>
Create CHANGELOG.md with hand-curated entries from git history in Keep a Changelog 1.1.0 format, covering the hardening milestone and initial release.

Purpose: Users need a human-readable summary of what caldawarrior 1.0.0 includes -- what was added, what changed, and what was fixed -- without wading through raw commit history.
Output: CHANGELOG.md at repository root.
</objective>

<execution_context>
@/home/alexandre/.claude/get-shit-done/workflows/execute-plan.md
@/home/alexandre/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-documentation-and-release/06-CONTEXT.md
@.planning/phases/06-documentation-and-release/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CHANGELOG.md with curated entries from git history</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - .planning/phases/06-documentation-and-release/06-RESEARCH.md (CHANGELOG Curation Guide section with commit-to-entry mapping)
    - .planning/ROADMAP.md (phase descriptions for understanding what each phase accomplished)
  </read_first>
  <action>
Read the git log to understand the full commit history:
```bash
git log --oneline
```

Create CHANGELOG.md at the repository root. Use Keep a Changelog 1.1.0 format (https://keepachangelog.com/en/1.1.0/).

The file must contain exactly this structure:

```markdown
# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [1.0.0] - 2026-03-XX

### Added

- Bidirectional sync between TaskWarrior 3.x and CalDAV VTODO calendars with last-write-wins conflict resolution
- Task dependency sync via `RELATED-TO;RELTYPE=DEPENDS-ON` (RFC 9253) -- unique among CalDAV sync tools
- Dry-run mode (`--dry-run`) to preview sync changes without writing
- `--fail-fast` flag to stop on first sync error instead of continuing
- Cyclic dependency detection with graceful handling -- cyclic tasks sync all fields except relations
- CalDAV CATEGORIES bidirectional mapping to TaskWarrior tags
- DATE-only DUE/DTSTART value preservation through sync round-trips
- Timezone-aware datetime handling with DST ambiguity resolution
- Non-standard iCalendar property preservation (X-APPLE-SORT-ORDER, X-OC-HIDESUBTASKS, etc.)
- Pre-built x86_64 Linux binary published to GitHub Releases on each tagged version
- CI pipeline with cargo fmt, clippy, unit tests, integration tests, Robot Framework E2E tests, and cargo-deny security audit

### Changed

- Cyclic dependency handling: cyclic tasks now sync all fields except RELATED-TO (previously entire task was skipped)
- Task update mechanism uses `task modify` with tag/annotation diff instead of `task import` (fixes caldavuid UDA loss in Docker TW3)

### Fixed

- CATEGORIES comma-escaping: tags containing commas no longer silently split into separate tags
- XML parser: replaced with namespace-aware quick-xml parser, fixing compatibility with non-Radicale CalDAV servers (Nextcloud, Baikal)
- ETag normalization: weak ETags from Nextcloud/Baikal handled correctly, preventing 412 Precondition Failed loops
- Error context: sync errors now include task UUID, field name, and server URL instead of swallowed defaults
- CANCELLED status propagation: fixed asymmetry in CalDAV-to-TaskWarrior deletion handling
- DST fall-back ambiguity resolved to standard-time interpretation; spring-forward gap falls back to UTC
```

**IMPORTANT guidelines for the CHANGELOG content:**
- This is the first release (1.0.0), so the "Added" section lists the core capabilities that ship with v1
- Include the initial features (sync, LWW, dry-run) as "Added" because users have no prior version to compare against
- "Changed" covers behavior modifications made during hardening (cyclic deps handling, task update mechanism)
- "Fixed" covers bug fixes from the hardening milestone (Phases 1-4)
- Do NOT include: internal planning commits (docs(01), docs(02), etc.), test infrastructure (Robot Framework setup, CATALOG.md), foundry/spec tooling, phase summaries
- Do NOT include "Removed" or "Deprecated" sections (nothing was removed or deprecated)
- Do NOT include "Security" section (cargo-deny is infrastructure, not a user-facing security fix)
- Use `2026-03-XX` as the date placeholder -- user fills the exact date when tagging the release
- Aim for 10-15 total entries across all sections, grouped by user impact
- Each entry should be a single line starting with `- ` (dash space)
  </action>
  <verify>
    <automated>test -f CHANGELOG.md && grep -c '## \[1.0.0\]' CHANGELOG.md && grep -c '### Added' CHANGELOG.md && grep -c '### Changed' CHANGELOG.md && grep -c '### Fixed' CHANGELOG.md && grep -c 'Keep a Changelog' CHANGELOG.md && wc -l < CHANGELOG.md</automated>
  </verify>
  <acceptance_criteria>
    - CHANGELOG.md exists at repository root
    - First line is `# Changelog`
    - Contains `The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/)`
    - Contains `and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html)`
    - Contains exactly one version header: `## [1.0.0] - 2026-03-XX`
    - Contains `### Added` section with entries for: bidirectional sync, dependency sync (RFC 9253), dry-run, --fail-fast, cyclic detection, CATEGORIES mapping, DATE-only preservation, timezone handling, X-property preservation, binary releases, CI pipeline
    - Contains `### Changed` section with entries for: cyclic dependency handling change, task update mechanism change
    - Contains `### Fixed` section with entries for: CATEGORIES comma-escaping, XML parser, ETag normalization, error context, CANCELLED propagation, DST handling
    - Does NOT contain `### Removed` or `### Deprecated` or `### Security`
    - Does NOT contain strings: `docs(`, `CATALOG.md`, `foundry`, `SUMMARY`, `PLAN.md`, `Robot Framework` (as a changelog entry -- the CI entry can mention E2E tests)
    - File has at least 30 lines
    - Date placeholder is `2026-03-XX` (not a specific date)
  </acceptance_criteria>
  <done>CHANGELOG.md exists with Keep a Changelog format, version header [1.0.0] - 2026-03-XX, and 10-15 hand-curated entries across Added/Changed/Fixed sections describing user-facing changes only.</done>
</task>

</tasks>

<verification>
1. `head -5 CHANGELOG.md` -- correct header and format declaration
2. `grep '## \[1.0.0\]' CHANGELOG.md` -- version header with date placeholder
3. `grep -c '^- ' CHANGELOG.md` -- count of entries (expect 10-15)
4. `grep -E 'docs\(|CATALOG|foundry|SUMMARY|PLAN\.md' CHANGELOG.md` -- must return empty (no internal entries)
5. `grep 'DEPENDS-ON' CHANGELOG.md` -- dependency sync mentioned
6. `grep 'fail-fast' CHANGELOG.md` -- new CLI flag mentioned
</verification>

<success_criteria>
- CHANGELOG.md follows Keep a Changelog 1.1.0 format exactly
- Version header uses 2026-03-XX date placeholder
- Added section covers all v1 capabilities (sync, dependencies, CLI flags, compatibility features, CI/release infrastructure)
- Changed section covers behavior modifications (cyclic deps, task update mechanism)
- Fixed section covers all bug fixes from hardening (CATEGORIES, XML, ETag, errors, CANCELLED, DST)
- No internal/planning/test-infrastructure entries leak into the CHANGELOG
- 10-15 total entries, each describing user-facing impact
</success_criteria>

<output>
After completion, create `.planning/phases/06-documentation-and-release/06-02-SUMMARY.md`
</output>
